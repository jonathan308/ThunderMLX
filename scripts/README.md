# Scripts

Secondary helper scripts live here. Most users should prefer
`M3_Start.command`, `M3_Stop.command`, and the dashboard.

- `download_model.sh`: download a model to rank 0 and optionally rank 1.
- `run_server.sh`: single-node/local fallback server launcher.
- `test_endpoint.sh`: legacy self-contained endpoint smoke script.
